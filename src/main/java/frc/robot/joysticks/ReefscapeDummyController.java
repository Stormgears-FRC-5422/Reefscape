package frc.robot.joysticks;

public class ReefscapeDummyController extends ReefscapeJoystick {
    // we need to be able to create an instance of the abstract base class, but
    // usually want to use its implementations
}
